# Activate with SPRING_PROFILES_ACTIVE=local after database bootstrap and credential provisioning.
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:fresveg}}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:091653}
  liquibase:
    enabled: ${LIQUIBASE_ENABLED:false}
logging:
  level:
    com.fresveg: ${FRESVEG_LOG_LEVEL:DEBUG}

springdoc:
  api-docs:
    enabled: ${API_DOCS_ENABLED:true}

fulfillment:
  account-base-url: ${ACCOUNT_BASE_URL:http://localhost:8081}
  commerce-base-url: ${COMMERCE_BASE_URL:http://localhost:8084}
  security:
    issuer-uri: ${OIDC_ISSUER_URI:http://localhost:8180/realms/fresveg}
    audience: ${OIDC_AUDIENCE:fresveg-fulfillment}
    jwk-set-uri: ${OIDC_JWK_SET_URI:http://localhost:8180/realms/fresveg/protocol/openid-connect/certs}
